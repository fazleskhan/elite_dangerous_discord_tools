@startuml
actor User
participant DiscordContext as Ctx
participant EDDiscordBot as Bot
participant ILogger as Logger

User -> Bot: ping(ctx)
Bot -> Logger: debug("Received ping command")
Bot -> Ctx: send("Pong (N ms)")
@enduml
